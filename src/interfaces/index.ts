import type { IBookmark } from "./bookmark";
import type { IWeatherResponse } from "./weather";
import type { IStickyNote } from "./notes";
import type {
  ISearchBarWidget,
  IDateAndTimeWidget,
  IBookmarksWidget,
  IStickyNotesWidget,
} from "./widgets";

export type {
  IBookmark,
  IWeatherResponse,
  ISearchBarWidget,
  IDateAndTimeWidget,
  IBookmarksWidget,
  IStickyNotesWidget,
  IStickyNote,
};
